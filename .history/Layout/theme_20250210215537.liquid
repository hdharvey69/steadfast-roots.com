<!DOCTYPE html>
    <html lang="en">
    <head>
        <!-- Metadata for SEO & responsiveness -->
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="{{ shop.description }}">
        
        <!-- Dynamic Page Title -->
        <title>{{ page_title }} - {{ shop.name }}</title>
    
        <!-- Link to Shopify's main theme stylesheet -->
        <link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">
    
        <!-- Ensure theme.css is loaded correctly -->
        <noscript>
          <style>
            body { background: #222; color: white; font-family: Arial, sans-serif; padding: 20px; }
          </style>
        </noscript>
    
        <!-- Load Additional Styles for Product Pages -->
        {% if template contains 'product' %}
          <style>
            .product-page { background: #2e2e2e; color: white; padding: 20px; }
          </style>
        {% endif %}
    
        <!-- SEO Structured Data -->
        <script type="application/ld+json">
        {
          "@context": "https://schema.org",
          "@type": "Store",
          "name": "{{ shop.name }}",
          "url": "https://{{ shop.domain }}",
          "logo": "{{ shop.logo | image_url: 'medium' }}",
          "description": "{{ shop.description }}"
        }
        </script>
    
        <!-- Shopify Required Head Assets -->
        {{ content_for_header }}
    </head>
    <body>
        <!-- Include Header Section -->
        {% section 'header' %}
    
        <!-- Main Content Area -->
        <main>
            {{ content_for_layout }}
        </main>
    
        <!-- Include Footer Section with Warning if Missing -->
        {% if settings.footer_enabled != blank %}
          {% section 'footer' %}
        {% else %}
          <p style="color: red; text-align: center;">⚠️ Footer is missing. Please add a footer section.</p>
        {% endif %}
    
        <!-- Link JavaScript Files -->
        <script src="{{ 'theme.js' | asset_url }}" defer></script>
    
        <!-- Ensure JS Loads -->
        <script>
          if (!document.querySelector('script[src*="theme.js"]')) {
            console.warn("⚠️ theme.js is missing. Please check the assets folder.");
          }
        </script>
    </body>
    </html>
    